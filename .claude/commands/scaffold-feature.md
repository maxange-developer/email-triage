---
description: "Scaffold: Zod schema + types + DB migration + actions + UI + page + tests"
argument-hint: <feature-name>
---
Show plan — wait for approval before creating files.
1. src/lib/validations/$ARGUMENTS.ts · src/lib/types/$ARGUMENTS.ts
2. supabase/migrations/[ts]_$ARGUMENTS.sql (if DB needed)
3. src/app/[route]/actions.ts · src/app/$ARGUMENTS/page.tsx
4. src/components/$ARGUMENTS/ · tests/unit/$ARGUMENTS.test.ts
